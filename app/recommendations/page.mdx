---
title: Recommendations for Mohsin Hayat
description: Verified recommendations for Mohsin Hayat from engineering leaders and collaborators.
---

import { profile, recommendationUrl } from '../../data/profile'

<main id="main-content" className="recommendations-page">

Perspectives from colleagues and engineering leaders I have worked with internationally.

<div className="recommendation-grid full-page">
  {profile.recommendations.map((item) => (
    <article className="recommendation-card" key={item.name}>
      <p>“{item.excerpt}”</p>
      <footer>
        <strong>{item.name}</strong>
        <span>{item.role}</span>
        <a href={recommendationUrl} target="_blank" rel="noreferrer">
          View LinkedIn recommendation{' '}
          <span className="sr-only">(opens in a new tab)</span>
        </a>
      </footer>
    </article>
  ))}
</div>

<details id="earlier-recommendations" className="earlier-recommendations">
  <summary>
    <span>View earlier recommendations</span>
    <span>Early freelance feedback and an employer reference</span>
  </summary>

  <div className="earlier-recommendations-content">
    <p>
      These references are from earlier work and are retained as supporting
      context alongside the senior-level recommendations above.
    </p>
    <div className="recommendation-grid">
      {profile.earlierRecommendations.map((item) => (
        <article className="recommendation-card" key={item.name}>
          <p>“{item.excerpt}”</p>
          <footer>
            <strong>{item.name}</strong>
            <span>{item.role}</span>
          </footer>
        </article>
      ))}
    </div>
    <a
      className="text-link earlier-reference-link"
      href={profile.earlierRecommendationLetter.href}
    >
      {profile.earlierRecommendationLetter.label}
    </a>
  </div>
</details>

</main>
